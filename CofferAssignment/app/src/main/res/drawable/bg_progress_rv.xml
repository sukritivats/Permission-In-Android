<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/lightDullColor"/> <!-- Customize the progress color -->
                <corners android:radius="5dp"/> <!-- Customize the corner radius -->
            </shape>
        </clip>
    </item>
</layer-list>
